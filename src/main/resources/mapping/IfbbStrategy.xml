<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.springmvc.mapper.IfbbStrategyMapper">

	<resultMap id="BaseResultMap" type="com.springmvc.po.IfbbStrategy">
		<id column="id" property="id"   />
		<result column="content" property="content"  />
		<result column="image" property="image"  />
	</resultMap>

	<sql id="Base_Column_List">
		id, content, image
	</sql>






 
	<!-- 按条件新增对象 -->
    <insert id="insert" parameterType="com.springmvc.po.IfbbStrategy">
        insert into ifbb_strategy
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="content != null">
                content,
            </if>
            <if test="image != null">
                image,
            </if>
        </trim>

        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id },
            </if>
            <if test="content != null">
                #{content },
            </if>
            <if test="image != null">
                #{image },
            </if>
        </trim>
    </insert>

    <insert id="insertSelective" parameterType="com.springmvc.po.IfbbStrategy">
        insert into ifbb_strategy
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="content != null">
                content,
            </if>
            <if test="image != null">
                image,
            </if>
        </trim>

        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id },
            </if>
            <if test="content != null">
                #{content },
            </if>
            <if test="image != null">
                #{image },
            </if>
        </trim>
    </insert>

    <!-- 根据编号删除 -->
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
        delete from ifbb_strategy  where  id  =#{id}
    </delete>

    <!-- 按条件更新对象 -->
    <update id="updateByPrimaryKeySelective" parameterType="com.springmvc.po.IfbbStrategy">
        update ifbb_strategy
        <set>
            <if test="id != null">
                id = #{id},
            </if>
            <if test="content != null">
                content = #{content},
            </if>
            <if test="image != null">
                image = #{image},
            </if>
        </set>
        where id = #{id}
    </update>

    <!-- 分页查询 -->
    <select id="selectIfbbStrategyByPage" resultMap="BaseResultMap">
        select
          *
        from ifbb_strategy
        where
         1=1
            <if test="ifbbStrategy.id != null and ifbbStrategy.id != '' ">
                and id = #{ifbbStrategy.id}
            </if>
            <if test="ifbbStrategy.content != null and ifbbStrategy.content != '' ">
                and content = #{ifbbStrategy.content}
            </if>
            <if test="ifbbStrategy.image != null and ifbbStrategy.image != '' ">
                and image = #{ifbbStrategy.image}
            </if>
    </select>

    <!-- 查看用户详情 -->
    <select id="selectByPrimaryKey" resultMap="BaseResultMap">
        select
            <include refid="Base_Column_List" />
        from ifbb_strategy
        where id = #{id}
    </select>
 
</mapper>